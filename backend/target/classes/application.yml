server:
  port: ${PORT:8080}
spring:
  data:
    mongodb:
      uri: mongodb+srv://aryangupta457023_db_user:aryan1234@netranewscluster.xvrvbam.mongodb.net/netradb?retryWrites=true&w=majority&appName=NetraNewsCluster
gemini:
  api-key: ${GEMINI_API_KEY:}
  model: ${GEMINI_MODEL:gemini-2.0-flash}
app:
  cors-origins: ${CORS_ORIGINS:http://localhost:3000,http://localhost:5500,http://localhost:8080}
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG